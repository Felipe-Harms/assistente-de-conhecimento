# Runtime — pinned minor for reproducibility; resolver allows patch updates.
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*
pydantic-settings==2.6.*
asyncpg==0.30.*
SQLAlchemy==2.0.*
httpx==0.27.*
structlog==24.4.*
# pgvector Python types (asyncpg codec) for vector column round-trip.
pgvector==0.3.*
# PDF parsing (textual PDFs only; no OCR — DEC-002).
pypdf==5.*
# Required by FastAPI for multipart/form-data ingest.
python-multipart==0.0.*
